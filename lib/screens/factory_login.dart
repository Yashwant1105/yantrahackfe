import 'package:flutter/material.dart';

class FactoryLogin extends StatelessWidget {
  const FactoryLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
          child: Text('Factory Login Screen'),
        ),
      ),
    );
  }
}
