import 'package:flutter/material.dart';

class FactoryMain extends StatelessWidget {
  const FactoryMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
          child: Text('Factory main Screen'),
        ),
      ),
    );
  }
}
