import 'package:flutter/material.dart';

class FactorySignup extends StatelessWidget {
  const FactorySignup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
          child: Text('Factory Signup Screen'),
        ),
      ),
    );
  }
}
